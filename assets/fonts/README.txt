Drop a .ttf file here and name it "font.ttf" to use a custom font
(e.g. for CJK text support). main.py automatically picks it up if
present; otherwise it falls back to pygame's built-in default font.
